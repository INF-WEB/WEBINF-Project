import { Request, Response } from "express";
import { getRepository } from "typeorm";
import { validate } from "class-validator";



class CanidatesController {
    
    static getCanidates = async(req: Request, res: Response) => {

    };
}

export default CanidatesController;
